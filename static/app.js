alert("NEXORA JS LOADED");

console.log("NEXORA JS LOADED");

const send = document.getElementById("send");

if (send) {
    send.addEventListener("click", () => {
        alert("SEND BUTTON WORKS");
    });
}
